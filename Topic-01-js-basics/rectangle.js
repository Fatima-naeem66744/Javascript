"use strict";

// sample input 1
const length1 = 11;
const width1 = 5;
const area1 = length1 * width1;
const perimeter1 = 2 * (length1 + width1);
console.log(`The area of the rectangle is ${area1} square units.`);
console.log(`The perimeter of the rectangle is ${perimeter1} units.`);

// sample input 2
const length2 = 7.5;
const width2 = 3.2;
const area2 = length2 * width2;
const perimeter2 = 2 * (length2 + width2);
console.log(`The area of the rectangle is ${area2} square units.`);
console.log(`The perimeter of the rectangle is ${perimeter2} units.`);

// sample input 3
const length3 = 0;
const width3 = 0;
const area3 = length3 * width3;
const perimeter3 = 2 * (length3 + width3);
console.log(`The area of the rectangle is ${area3} square units.`);
console.log(`The perimeter of the rectangle is ${perimeter3} units.`);
